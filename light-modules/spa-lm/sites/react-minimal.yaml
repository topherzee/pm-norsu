templates:
  prototype:
    baseUrl: http://${env.BASE_URL}
    routeTemplate: '/{language}{{@path}}'
    $type: spaPrototype
mappings:
  website:
    handlePrefix: '/react-minimal'
    repository: ''